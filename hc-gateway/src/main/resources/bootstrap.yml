spring:
  application:
    name: hc-gateway
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:9010
      config:
        server-addr: ${nacos.url}:9010
        group: DEFAULT_GROUP
        prefix: hc-gateway
        file-extension: yaml