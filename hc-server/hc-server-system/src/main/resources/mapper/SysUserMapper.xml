<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hc.server.system.mapper.SysUserMapper">
    <select id="findUserDetailPage" parameterType="sysUser" resultType="sysUser">
        SELECT
        u.id,
        u.username,
        u.nickname,
        u.email,
        u.phone,
        u.status,
        u.create_time createTime,
        u.sex,
        u.dept_id deptId,
        u.last_login_time lastLoginTime,
        u.update_time updateTime,
        u.remark,
        u.avatar,
        d.dept_name deptName,
        GROUP_CONCAT(r.id) roleId,
        GROUP_CONCAT(r.role_name) roleName
        FROM
        t_user u
        LEFT JOIN t_dept d ON (u.dept_id = d.id)
        LEFT JOIN t_user_role ur ON (u.id = ur.user_id)
        LEFT JOIN t_role r ON r.id = ur.role_id
        WHERE 1 = 1
        <if test="user.username != null and user.username != ''">
            AND u.username like CONCAT('%',#{user.username},'%')
        </if>
        <if test="user.deptId != null and user.deptId != ''">
            AND d.dept_id = #{user.deptId}
        </if>
        <if test="user.sex != null and user.sex != ''">
            AND u.sex = #{user.sex}
        </if>
        <if test="user.status != null and user.status != ''">
            AND u.status = #{user.status}
        </if>
        <if test="user.phone != null and user.phone != ''">
            AND u.phone = #{user.phone}
        </if>
        <if test="user.deptName != null and user.deptName != ''">
            AND d.dept_name like CONCAT('%',#{user.deptName},'%')
        </if>
        <if test="user.createTimeFrom != null and user.createTimeFrom !=''">
            And u.create_time &gt; #{user.createTimeFrom}
        </if>
        <if test="user.createTimeTo!= null and user.createTimeTo !=''">
            And u.create_time &lt; #{user.createTimeTo}
        </if>
        group by u.username,u.id,u.email,u.phone,u.status,u.create_time,u.sex,
        u.dept_id,u.last_login_time,u.update_time,u.remark,u.avatar
    </select>
</mapper>