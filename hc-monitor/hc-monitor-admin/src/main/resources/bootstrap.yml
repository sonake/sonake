spring:
  application:
    name: hc-monitor-admin
  cloud:
    nacos:
      config:
        server-addr: ${nacos.url}:9010
        group: DEFAULT_GROUP
        prefix: hc-monitor-admin
        file-extension: yaml