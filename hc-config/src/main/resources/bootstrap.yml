server:
  port: 9050
spring:
  application:
    name: hc-config
  profiles:
    active: git
  #配置中心
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sonake/server-config.git
          search-paths: /sonake-cfg/
          username: 760946378@qq.com
          password: yxz0921..
  rabbitmq:
    host: 192.168.220.128
    port: 5672
    username: guest
    password: guest
eureka:
  instance:
    #向Eureka服务端发送心跳的间隔时间，单位为秒，用于服务续约。这里配置为20秒，即每隔20秒向hc-register发送心跳，表明当前服务没有宕机
    lease-expiration-duration-in-seconds: 20
  client:
    register-with-eureka: true
    fetch-registry: true
    instance-info-replication-interval-seconds: 30
    #默认值为30秒，即每30秒去Eureka服务端上获取服务并缓存，这里指定为3秒的原因是方便开发时测试，实际可以指定为默认值即可
    registry-fetch-interval-seconds: 3
    service-url:
      defaultZone: http://sonake:123456@127.0.0.1:9010/register/eureka/

management:
  endpoints:
    web:
      exposure:
        include: '*'
